'use strict';

import EventEmitter = require('events');

class Rtp extends EventEmitter {
    constructor(sessionID: any) {
        super();
    }

    // ********************  ********************
}

module.exports = {
    Rtp: Rtp
};